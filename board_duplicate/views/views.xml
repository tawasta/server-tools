<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="board_duplicate.list">
      <field name="name">board_duplicate list</field>
      <field name="model">board_duplicate.board_duplicate</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="board_duplicate.action_window">
      <field name="name">board_duplicate window</field>
      <field name="res_model">board_duplicate.board_duplicate</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="board_duplicate.action_server">
      <field name="name">board_duplicate server</field>
      <field name="model_id" ref="model_board_duplicate_board_duplicate"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="board_duplicate" id="board_duplicate.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="board_duplicate.menu_1" parent="board_duplicate.menu_root"/>
    <menuitem name="Menu 2" id="board_duplicate.menu_2" parent="board_duplicate.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="board_duplicate.menu_1_list" parent="board_duplicate.menu_1"
              action="board_duplicate.action_window"/>
    <menuitem name="Server to list" id="board_duplicate" parent="board_duplicate.menu_2"
              action="board_duplicate.action_server"/>
    -->
  </data>
</odoo>